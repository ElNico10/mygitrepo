{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run AVL Benchmark",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/avl_benchmark",
            "args": ["uszips.csv"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "build-with-make"
        }
    ]
}
